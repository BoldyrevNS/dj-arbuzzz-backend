# Contributing to DJ Arbuzzz

Спасибо за интерес к проекту! 🎉

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Начало работы](#начало-работы)
- [Как внести вклад](#как-внести-вклад)
- [Стиль кода](#стиль-кода)
- [Процесс Pull Request](#процесс-pull-request)
- [Сообщение об ошибках](#сообщение-об-ошибках)

## 🤝 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь с нашим [Code of Conduct](CODE_OF_CONDUCT.md).

## 🚀 Начало работы

### Настройка окружения для разработки

1. **Fork и клонирование репозитория**

```bash
git clone https://github.com/YOUR_USERNAME/dj-arbuzzz-backend.git
cd dj-arbuzzz-backend
```

2. **Установка зависимостей**

```bash
make setup
```

3. **Запуск в режиме разработки**

```bash
# Вариант 1: Полный Docker стек
make dev

# Вариант 2: Локальная разработка
make dev-infra  # Только инфраструктура
make dev-backend  # В отдельном терминале
make dev-frontend  # В другом терминале
```

## 💡 Как внести вклад

### Типы вкладов

- 🐛 **Bug fixes** - исправление ошибок
- ✨ **Features** - новая функциональность
- 📚 **Documentation** - улучшение документации
- 🎨 **UI/UX** - улучшение интерфейса
- ⚡ **Performance** - оптимизация производительности
- ✅ **Tests** - добавление/улучшение тестов

### Процесс разработки

1. **Создайте issue** (если его еще нет)
   - Опишите проблему или предложение
   - Дождитесь обсуждения и одобрения

2. **Создайте ветку из `main`**

```bash
git checkout main
git pull origin main
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

3. **Внесите изменения**
   - Пишите чистый, идиоматичный код
   - Обновите документацию при необходимости

4. **Следуйте стилю кода** (см. ниже)

5. **Коммитьте изменения**

```bash
git add .
git commit -m "feat: add amazing feature"
```

6. **Запушьте ветку**

```bash
git push origin feature/your-feature-name
```

7. **Откройте Pull Request**

## 📝 Стиль кода

### Commits

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Types:**
- `feat:` - новая функциональность
- `fix:` - исправление ошибки
- `docs:` - документация
- `style:` - форматирование
- `refactor:` - рефакторинг
- `perf:` - производительность
- `test:` - тесты
- `chore:` - рутинные задачи
- `ci:` - CI/CD

**Примеры:**
```bash
feat(backend): add user authentication
fix(frontend): resolve login form validation
docs(readme): update installation instructions
```

### Rust (Backend)

```bash
# Форматирование
cd server
cargo fmt

# Линтинг
cargo clippy -- -D warnings
```

**Стандарты:**
- Используйте `rustfmt` для форматирования
- Все предупреждения clippy должны быть исправлены
- Документируйте публичные API с `///`
- Используйте `Result<T, E>` для обработки ошибок

### TypeScript/Vue (Frontend)

```bash
# Линтинг
cd client
pnpm run lint

# Автоисправление
pnpm run lint:fix
```

**Стандарты:**
- Используйте TypeScript для всего кода
- Следуйте Vue Style Guide
- Используйте Composition API
- Компоненты должны быть типизированы
- Props должны иметь валидацию

### Общее

- Делайте маленькие, фокусированные коммиты
- Пишите ясные описания коммитов
- Комментируйте сложную логику
- Удаляйте неиспользуемый код
- Не коммитьте закомментированный код

## 🔄 Процесс Pull Request

### Checklist перед созданием PR

- [ ] Код соответствует стилю проекта
- [ ] Линтинг проходит (`make lint`)
- [ ] Документация обновлена
- [ ] Нет конфликтов с `main`
- [ ] Commit messages следуют Conventional Commits
- [ ] PR description заполнен

### Шаблон PR

При создании PR автоматически появится [шаблон](.github/pull_request_template.md). Заполните все разделы:

- Описание изменений
- Связанные issues
- Тип изменений
- Checklist
- Инструкции по тестированию

### Процесс review

1. **Автоматические проверки**
   - CI должен пройти успешно
   - Код должен быть отформатирован

2. **Code review**
   - Минимум один одобрение от maintainer
   - Все комментарии должны быть разрешены
   - Конфликты должны быть разрешены

3. **Merge**
   - После одобрения PR будет смержен
   - Ветка будет автоматически удалена

## 🐛 Сообщение об ошибках

### Перед созданием bug report

1. **Проверьте существующие issues**
   - Возможно, проблема уже зарегистрирована

2. **Убедитесь, что это баг**
   - Проверьте документацию
   - Попробуйте воспроизвести на последней версии

3. **Соберите информацию**
   - Версия проекта
   - ОС и версия
   - Шаги для воспроизведения
   - Логи ошибок

### Создание bug report

Используйте [bug report template](.github/ISSUE_TEMPLATE/bug_report.yml):

1. Перейдите в Issues
2. Нажмите "New Issue"
3. Выберите "🐛 Bug Report"
4. Заполните все поля
5. Прикрепите скриншоты/логи
6. Отправьте

## ✨ Предложение новой функциональности

Используйте [feature request template](.github/ISSUE_TEMPLATE/feature_request.yml):

1. Опишите проблему, которую решает функция
2. Предложите решение
3. Опишите альтернативы
4. Добавьте примеры использования
5. Укажите, готовы ли реализовать сами

## 📊 Разработка

### Структура проекта

```
.
├── server/          # Rust backend
│   ├── src/
│   ├── migrations/
│   └── Cargo.toml
├── client/          # Nuxt frontend
│   ├── app/
│   ├── server/
│   └── package.json
├── nginx/           # Nginx конфигурация
├── scripts/         # Утилиты
├── .github/         # CI/CD
└── docker-compose.yml
```

### Полезные команды

```bash
make help              # Показать все команды
make setup             # Настроить проект
make dev               # Запустить в режиме разработки
make lint              # Проверить код
make fix               # Исправить форматирование
make docker-build      # Собрать Docker образы
```

### Отладка

**Backend:**
```bash
# Логи
cd server
RUST_LOG=debug cargo run

# Отладка с lldb
rust-lldb target/debug/backend-rust
```

**Frontend:**
```bash
# Dev tools
cd client
pnpm dev

# Отладка в браузере
# Chrome DevTools или Vue DevTools
```

**Docker:**
```bash
# Логи сервиса
docker-compose logs -f backend

# Войти в контейнер
docker-compose exec backend sh
```

## 📚 Дополнительные ресурсы

- [Rust Book](https://doc.rust-lang.org/book/)
- [Axum Documentation](https://docs.rs/axum/)
- [Nuxt Documentation](https://nuxt.com/)
- [Vue 3 Documentation](https://vuejs.org/)
- [Docker Documentation](https://docs.docker.com/)

## ❓ Вопросы?

- Откройте [Discussion](https://github.com/YOUR_USERNAME/dj-arbuzzz-backend/discussions)
- Спросите в issue
- Свяжитесь с maintainers

## 🙏 Спасибо!

Ваш вклад делает этот проект лучше! 🎉
